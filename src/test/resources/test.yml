multiDatabase:
  tenantDbMap:
    shard1:
      driverClass: "com.mysql.jdbc.Driver"
      user: "root"
      password: "password"
      url: "jdbc:mysql://localhost:3306/gst_stage_db"
      maxWaitForConnection: 1s
      initialSize: 10
      minSize: 10
      maxSize: 100
      defaultTransactionIsolation: READ_COMMITTED
      checkConnectionWhileIdle: false
      checkConnectionOnBorrow: true
      properties:
        charSet: utf8mb4
        characterEncoding: UTF-8
        useUnicode: true
        hibernate.dialect: org.hibernate.dialect.MySQLDialect
        org.hibernate.envers.do_not_audit_optimistic_locking_field: false
        org.hibernate.envers.audit_table_suffix: "_aud"
        hibernate.hbm2ddl.auto: validate
        #    hibernate.hbm2ddl.auto: create
        hibernate.session.events.log: false
        hibernate.create_empty_composites.enabled: true # Don't set embeddable to NULL, when all it's fields are null
    shard2:
      driverClass: "com.mysql.jdbc.Driver"
      user: "root"
      password: "password"
      url: "jdbc:mysql://localhost:3306/gst_stage_db2"
      maxWaitForConnection: 1s
      initialSize: 10
      minSize: 10
      maxSize: 100
      defaultTransactionIsolation: READ_COMMITTED
      checkConnectionWhileIdle: false
      checkConnectionOnBorrow: true
      properties:
        charSet: utf8mb4
        characterEncoding: UTF-8
        useUnicode: true
        hibernate.dialect: org.hibernate.dialect.MySQLDialect
        org.hibernate.envers.do_not_audit_optimistic_locking_field: false
        org.hibernate.envers.audit_table_suffix: "_aud"
        hibernate.hbm2ddl.auto: validate
        #    hibernate.hbm2ddl.auto: create
        hibernate.session.events.log: false
        hibernate.create_empty_composites.enabled: true # Don't set embeddable to NULL, when all it's fields are null
#    shard1:
#      driverClass: org.h2.Driver
#      url: "jdbc:h2:mem:test;MODE=MySQL;DATABASE_TO_UPPER=false"
#      properties:
#        charSet: UTF-8
#        hibernate.dialect: org.hibernate.dialect.H2Dialect
#        #    hibernate.dialect: org.hibernate.dialect.MySQLDialect
#        hibernate.create_empty_composites.enabled: true # Don't set embeddable to NULL, when all it's fields are null
#        hibernate.session.events.log: false
#        org.hibernate.envers.do_not_audit_optimistic_locking_field: false
#        org.hibernate.envers.audit_table_suffix: "_aud"
#    shard2:
#      driverClass: org.h2.Driver
#        url: "jdbc:h2:mem:test2;MODE=MySQL;DATABASE_TO_UPPER=false"
#        properties:
#          charSet: UTF-8
#          hibernate.dialect: org.hibernate.dialect.H2Dialect
#          #    hibernate.dialect: org.hibernate.dialect.MySQLDialect
#          hibernate.create_empty_composites.enabled: true # Don't set embeddable to NULL, when all it's fields are null
#          hibernate.session.events.log: false
#          org.hibernate.envers.do_not_audit_optimistic_locking_field: false
#          org.hibernate.envers.audit_table_suffix: "_aud"
  defaultTenant: "shard1"
#  properties:
#    charSet: utf8mb4
#    characterEncoding: UTF-8
#    useUnicode: true
#    hibernate.dialect: org.hibernate.dialect.MySQLDialect
#    org.hibernate.envers.do_not_audit_optimistic_locking_field: false
#    org.hibernate.envers.audit_table_suffix: "_aud"
#    hibernate.hbm2ddl.auto: validate
#    #    hibernate.hbm2ddl.auto: create
#    hibernate.session.events.log: false
#    hibernate.create_empty_composites.enabled: true # Don't set embeddable to NULL, when all it's fields are null
#    hibernate.show_sql: true
#    hibernate.format_sql: true